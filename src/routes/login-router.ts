import { Router } from "express";
// import LoginController from "../controllers/login-controller";
// import LoginrMiddleware from "../middlewares/login-middleware";

const loginRouter: Router = Router();




export default loginRouter;